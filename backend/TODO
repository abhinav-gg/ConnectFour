
i dont even want to know what this is a key for:
"%I*He2I2@@4uclA6&oB5IAH@W^wl7yf9xu0I#Fzw#kW8#!5mi55GtJY^N0xcB4^S"

play 2 mid tier bots against each other all night and detect specific conditions in a position E.G. 2-3 back to back only winning moves in the positions.



Disconnects start job
Reconnect should remove job from queue
Player moves are fine
Assume 100% disconnection detection so no need for general game tracking


restructure live game ui

-BACKEND TESTS
npx tsc -p .\tsconfig.json --noEmit


npx ts-node -r tsconfig-paths/register ./tests/index.ts
------------


tsconfig include all
"./**/*",

Dev Cmds:
ssh -i C:\ProgramData\ssh\Con4User.pem -N -L 5432:con4-relational.c096oucyo5dq.us-east-1.rds.amazonaws.com:5432 ec2-user@44.196.230.60

psql -h con4-relational.c096oucyo5dq.us-east-1.rds.amazonaws.com -U postgres -d con4db -p 5432

v DELETES REDIS AS WELL
docker-compose down --volumes --remove-orphans

docker-compose down
docker-compose up --build

v Re-Run without overhead
docker-compose start

v don't be an idiot and LOSE the docker stuff
docker cp express-api:/app/data/opening-book.json ./saved-openings.json



docker login ghcr.io -u con4-app --password-stdin < token.txt
docker pull ghcr.io/abhinav-gg/connectfour:latest
