# Save snapshot every 15 minutes if at least 1 key changed
save 900 1

# Disable AOF for simplicity (optional)
appendonly no

# Set max memory to 100mb (adjust as needed)
maxmemory 100mb

# Eviction policy: volatile-lru (evict keys with expiry set, least recently used)
maxmemory-policy volatile-lru