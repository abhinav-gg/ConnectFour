services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: redis
    env_file:
      - ./backend/.env
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "defaultdevpassword", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - redis-data:/data
      - ./backend/redis.dev.conf:/usr/local/etc/redis/redis.conf:ro
    restart: unless-stopped
    ports:
      - "6379:6379"
      - "8001:8001"
    command: ["redis-server",  "/usr/local/etc/redis/redis.conf"]
  
  api:
    build: 
      context: .
      dockerfile: backend.Dockerfile
    container_name: express-api
    restart: always
    ports:
      - "3001:3001"
      - "3002:3002"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
    env_file:
      - ./backend/.env

volumes:
  redis-data:
